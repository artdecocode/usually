<types>
  <type name="Config">
    <prop name="usage" type="Object<string, string>">
      The map with possible arguments (flags, options) and their information.
    </prop>
    <prop opt string name="description">
      What does the program do?
    </prop>
    <prop opt string name="line">
      The summary of the program in one line.
    </prop>
    <prop opt string name="example">
      The Example of the program usage.
    </prop>
  </type>
</types>